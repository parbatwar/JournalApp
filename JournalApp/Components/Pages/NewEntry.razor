@page "/"
@layout MainLayout
@using JournalApp.Models
@using Microsoft.EntityFrameworkCore
@using JournalApp.Services
@inject AppDbContext Db
@inject Session Session
@inject NavigationManager Nav
@inject IJSRuntime JS
@inject IEntryService EntryService

<div class="page-header">
    <h1>New Journal Entry</h1>
    <p>Write your thoughts and feelings for today</p>
</div>

<div class="content-card mb-4">

    <!-- Entry Date -->
    <div class="mb-4 col-md-4">
        <label class="form-label">Entry Date</label>
        <input type="date"
               class="form-control"
               @bind="entryDate" />
    </div>

    <!-- Title -->
    <div class="mb-4">
        <label class="form-label">Title</label>
        <input type="text"
               class="form-control"
               placeholder="Enter title"
               @bind="title" />
    </div>

    <!-- Content -->
    <div class="mb-5">
        <label class="form-label">Your Thoughts</label>
        <div id="quillEditor" style="height:200px;background:white;"></div>
    </div>

    <!-- Primary Mood -->
    <div class="mb-4">
        <p class="section-title">Primary Mood</p>
        <div class="d-flex flex-wrap gap-2">
            @foreach (var mood in moods)
            {
                <button type="button"
                        class="btn @(primaryMoodId == mood.MoodId ? "btn-dark" : "btn-outline-dark")"
                        @onclick="() => primaryMoodId = mood.MoodId">
                    @mood.MoodName
                </button>
            }
        </div>
    </div>

    <!-- Secondary Mood -->
    <div class="mb-4">
        <p class="section-subtitle">Secondary Mood (optional)</p>
        <div class="d-flex flex-wrap gap-2">
            @foreach (var mood in moods)
            {
                <button type="button"
                        class="btn @(secondaryMoodId == mood.MoodId ? "btn-secondary" : "btn-outline-secondary")"
                        @onclick="() => secondaryMoodId = mood.MoodId">
                    @mood.MoodName
                </button>
            }
        </div>
    </div>

    <!-- Tag -->
    <div class="mb-4">
        <label class="form-label">Category</label>
        <select class="form-select" @bind="selectedTagId">
            <option value="0">Choose...</option>
            @foreach (var tag in tags)
            {
                <option value="@tag.TagId">@tag.TagName</option>
            }
        </select>
    </div>

    <!-- Save Button -->
    <div class="d-grid mt-4">
        <button class="btn create-btn" @onclick="SaveEntry">
            Create Entry
        </button>
    </div>

</div>

@code {

    // form fields
    string title = "";
    DateTime entryDate = DateTime.Today;
    int primaryMoodId = 0;
    int secondaryMoodId = 0;
    int selectedTagId = 0;

    // data
    List<Mood> moods = new();
    List<Tag> tags = new();

    protected override async Task OnInitializedAsync()
    {
        if (Session.CurrentUser == null)
        {
            Nav.NavigateTo("/login");
            return;
        }

        moods = await Db.Moods.AsNoTracking().ToListAsync();
        tags = await Db.Tags.AsNoTracking()
                            .Where(t => t.TagType == TagTypeEnum.Predefined)
                            .ToListAsync();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JS.InvokeVoidAsync("initQuill");
        }
    }

    async Task SaveEntry()
    {
        // Title validation
        if (string.IsNullOrWhiteSpace(title))
        {
            var mainPage = Application.Current!.Windows[0].Page;
            if (mainPage != null)
            {
                await mainPage.DisplayAlertAsync(
                    "Alert",
                    "Title is required",
                    "OK"
                );
            }
            return;
        }

        if (primaryMoodId == 0)
        {
            var mainPage = Application.Current!.Windows[0].Page;
            if (mainPage != null)
            {
                await mainPage.DisplayAlertAsync(
                    "Alert",
                    "Please select a primary mood",
                    "OK"
                );
            }
            return;
        }

        if (selectedTagId == 0)
        {
            var mainPage = Application.Current!.Windows[0].Page;
            if (mainPage != null)
            {
                await mainPage.DisplayAlertAsync(
                    "Alert",
                    "Please select a category",
                    "OK"
                );
            }
            return;
        }

        // One entry per date check
        bool exists = await Db.Entries.AnyAsync(e =>
            e.UserId == Session.CurrentUser!.UserId &&
            e.EntryDate == entryDate
        );

        if (exists)
        {
            var mainPage = Application.Current!.Windows[0].Page;
            if (mainPage != null)
            {
                await mainPage.DisplayAlertAsync(
                    "Duplicate Entry",
                    "You already have an entry for this date",
                    "OK"
                );
            }
            return;
        }

        // Get editor content
        var content = await JS.InvokeAsync<string>("getQuillHtml");

        var entry = new JournalEntry
        {
            Title = title,
            Content = content,
            UserId = Session.CurrentUser!.UserId,
            TagId = selectedTagId,
            EntryDate = entryDate,
            CreatedAt = DateTime.Now,
            UpdatedAt = DateTime.Now
        };

        await EntryService.AddEntry(
            entry,
            primaryMoodId,
            secondaryMoodId == 0 ? null : secondaryMoodId
        );

        // ✅ SUCCESS ALERT (slide jastai)
        var mainPageSuccess = Application.Current!.Windows[0].Page;
        if (mainPageSuccess != null)
        {
            await mainPageSuccess.DisplayAlertAsync(
                "Success",
                "Journal entry created successfully",
                "OK"
            );
        }

        Nav.NavigateTo("/all-entries");
    }

}
